@{
    ViewBag.Title = "Listado de Alumnos";
}
@*con el reenderizado de vistas se manda a llamar esta vista al layout*@ 
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h3 class="mb-0">Alumnos Ollimani</h3>
        <button id="btnNuevo" class="btn btn-primary">Nuevo Alumno</button>
    </div>
    <div class="card-body">
        @*se manda a llamar al js el contenedor Grid y vizualizar los datos*@
        <div id="gridContainer"></div>
    </div>
</div>

@section scripts {
    <script src="~/Scripts/alumno.js"></script>
}